platform: linux

image_resource:
  type: docker-image
  source:
    repository: dockerdeepak/concourse
    tag: latest

inputs:
  - name: git-repo
  - name: mvn-package
outputs:
  - name: mvn-package-out

run:
  path: "ls"
  args: ['-alR']
